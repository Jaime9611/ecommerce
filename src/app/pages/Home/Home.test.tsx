import { render } from '@testing-library/react';
import Home from './Home';

// TODO: Finish test
describe.skip('Display product cards', () => {
  it('should display a list of products', () => {
    // TODO: make the context
    render(<Home />);
  });
});
