import React from 'react';
import { Container } from '@mui/material';

const Home = () => {
  return (
    <Container maxWidth='xl' sx={{ mt: 3 }}>
      Home
    </Container>
  );
};

export default Home;
